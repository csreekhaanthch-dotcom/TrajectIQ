# TrajectIQ Enterprise - Requirements
# ====================================
# Core dependencies for the desktop application

# Security
cryptography>=41.0.0
bcrypt>=4.0.0
argon2-cffi>=23.1.0
pyjwt>=2.8.0

# Database (encrypted)
pysqlcipher3>=1.2.0; sys_platform == 'linux'
# sqlcipher3 is installed separately on Windows

# HTTP (for cloud license validation and ATS connectors)
requests>=2.31.0
aiohttp>=3.9.0
certifi>=2024.2.0

# Desktop GUI
PyQt5>=5.15.0

# Resume parsing
PyPDF2>=3.0.0
python-docx>=0.8.11

# JSON Schema validation
jsonschema>=4.20.0

# Structured logging
python-json-logger>=2.0.7

# Build tools
pyinstaller>=6.0.0
setuptools>=68.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.2.0
hypothesis>=6.92.0  # For fuzz testing

# Code quality
flake8>=7.0.0
black>=24.1.0
mypy>=1.8.0
bandit>=1.7.0  # SAST

# Type stubs
types-requests>=2.31.0
